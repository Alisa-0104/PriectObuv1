<Window x:Class="ShoeStoreApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Магазин обуви" Height="700" Width="1200"
        WindowState="Maximized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="#2196F3" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Магазин обуви" 
                           FontSize="24" FontWeight="Bold" Foreground="White"/>

                <TextBlock Grid.Column="1" x:Name="UserInfoText" 
                           Text="Гость" FontSize="16" Foreground="White" 
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Меню -->
        <Menu Grid.Row="1">
            <MenuItem Header="Каталог" Click="ShowTovars_Click"/>
            <MenuItem Header="Пользователи" x:Name="MenuUsers" Click="ShowUsers_Click" Visibility="Collapsed"/>
            <MenuItem Header="Заказы" x:Name="MenuOrders" Click="ShowOrders_Click" Visibility="Collapsed"/>
            <MenuItem Header="Пункты выдачи" x:Name="MenuPickup" Click="ShowPickupPoints_Click" Visibility="Collapsed"/>
            <MenuItem Header="Выйти" Click="Logout_Click"/>
        </Menu>

        <!-- Контент -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="TovarsList" Margin="10">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" 
                                Margin="5" Padding="10" Width="250">
                            <StackPanel>
                                <Image Source="{Binding ImagePath}" 
                                       Height="150" Stretch="Uniform"/>
                                <TextBlock Text="{Binding ProductName}" 
                                           FontWeight="Bold" FontSize="14" 
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Article}" 
                                           Foreground="Gray"/>
                                <TextBlock Text="{Binding Price, StringFormat='{}{0:C}'}" 
                                           FontSize="16" Foreground="Green" 
                                           FontWeight="Bold"/>
                                <TextBlock Text="{Binding ProductDescription}" 
                                           TextWrapping="Wrap" FontSize="12"/>
                                <TextBlock Text="{Binding QuantityInStock, StringFormat='На складе: {0} шт.'}" 
                                           Foreground="Orange"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- DataGrid для других таблиц -->
        <DataGrid x:Name="MainDataGrid" Grid.Row="2" 
                  AutoGenerateColumns="True" IsReadOnly="True" 
                  Margin="10" Visibility="Collapsed"/>
    </Grid>
</Window>